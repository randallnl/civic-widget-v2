{
  "name": "nh-civic-commons-vote-tracker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:worker": "wrangler dev",
    "build": "vite build",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "worker:types": "wrangler types",
    "worker:dry-run": "wrangler deploy --dry-run"
  },
  "dependencies": {
    "lit": "^3.3.1"
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.27.0"
  }
}
